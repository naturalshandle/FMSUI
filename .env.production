# Used by `vite build` (Vercel). Local `npm run dev` keeps using .env / .env.local.
# A Vercel project env var of the same name overrides this.
VITE_API_BASE_URL=https://whatever-dad-develops-jerry.trycloudflare.com/api/v1
